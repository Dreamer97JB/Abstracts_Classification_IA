# Technology Stack

**Analysis Date:** 2026-03-24

## Languages

**Primary:**
- Python 3.11.11 - Main execution language in `AbstractsV2.ipynb` (`metadata.language_info.version`) for data cleaning, classification, topic modeling, NER, and export.

**Secondary:**
- JSON (Jupyter notebook format) - Notebook container format in `AbstractsV2.ipynb` and `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.
- HTML - Visualization outputs generated by BERTopic in `temas_interactivos.html` and `top_15_temas_bar.html`.
- CSV/XLSX - Canonical data interchange formats in `googleScholarPeriodAbs.xlsx`, `abstracs_cleaned.csv`, `seed_labeled.csv`, `seed_generated.csv`, `abstracts_reclasificados_top15.csv`, `abstracts_clasificados_subtemas_aprobados.csv`, `abstracts_clasificados_filosóficos.csv`, and `abstracts_con_metodologia_optimizado.csv`.

## Runtime

**Environment:**
- Jupyter notebook runtime - Local kernel `nlp-gpu` in `AbstractsV2.ipynb`.
- Google Colab runtime - Explicit in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb` via `from google.colab import drive` and `drive.mount('/content/drive')`.
- CUDA-aware PyTorch runtime - GPU/CPU branching is used repeatedly through `torch.cuda.is_available()` in both notebooks.

**Package Manager:**
- `pip` - Installed ad hoc inside notebooks with cells such as `pip install datasets`, `pip install setfit`, `pip install bertopic hdbscan umap-learn sentence-transformers`, `!pip install transformers==4.28.1`, and `!pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121`.
- Lockfile: missing

## Frameworks

**Core:**
- Pandas - Primary tabular processing engine used for `read_excel`, `read_csv`, filtering, type coercion, grouping, and `to_csv` in `AbstractsV2.ipynb` and `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.
- PyTorch - Inference/training backend and device selection layer used in both notebooks.
- Hugging Face Transformers - Used for zero-shot classification, NER pipelines, tokenization, and sequence-classification training in both notebooks.
- Hugging Face Datasets - Used to batch notebook pipelines with `Dataset.from_pandas(...)` and `.map(...)` in both notebooks.

**Testing:**
- Not detected

**Build/Dev:**
- Jupyter Notebook - Main authoring/execution environment in `AbstractsV2.ipynb`.
- Google Colab - Alternate execution environment and storage integration in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.

## Key Dependencies

**Critical:**
- `pandas` - Required to ingest `googleScholarPeriodAbs.xlsx` and all generated CSV stages.
- `torch` - Required for GPU acceleration and model execution paths.
- `transformers` - Required for `pipeline(...)`, `AutoTokenizer`, and `AutoModelForSequenceClassification`.
- `datasets` - Required for batched inference and training data handling.
- `setfit` - Used in `AbstractsV2.ipynb` for the SetFit classifier ensemble (`SetFitModel`, `Trainer`, `TrainingArguments`).
- `sentence-transformers` - Used to build embeddings for BERTopic and semantic reassignment.
- `bertopic` - Used to generate topic models and HTML visualizations.
- `nltk` - Used for sentence tokenization during segment voting in `AbstractsV2.ipynb`.

**Infrastructure:**
- `scikit-learn` - Used for `train_test_split`, `TfidfVectorizer`, and `cosine_similarity` across both notebooks.
- `evaluate` - Used only in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb` to compute accuracy and macro F1 for `xlm-roberta-base`.
- `umap-learn` and `hdbscan` - Used in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb` for BERTopic clustering; `AbstractsV2.ipynb` disables custom HDBSCAN with `hdbscan_model=None`.
- `matplotlib` and `seaborn` - Used for descriptive analytics and trend plots in `AbstractsV2.ipynb`.
- `plotly` - Used in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb` and indirectly by BERTopic visualizations.
- `spacy` and `wordcloud` - Imported in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`; active usage is exploratory/auxiliary rather than central to the current local pipeline.

## Model Dependencies

**Remote models referenced in notebook code:**
- `facebook/bart-large-mnli` - Zero-shot classification backbone in both notebooks.
- `sentence-transformers/paraphrase-mpnet-base-v2` - Embedding backbone for SetFit and BERTopic in `AbstractsV2.ipynb`.
- `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` - Embedding model in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.
- `xlm-roberta-base` - Fine-tuned supervised classifier in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.
- `dbmdz/bert-large-cased-finetuned-conll03-english` - NER model in `AbstractsV2.ipynb`.
- `microsoft/deberta-v3-large` - Experimental alternative zero-shot classifier in `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.

## Data Pipeline Artifacts

**Primary source:**
- `googleScholarPeriodAbs.xlsx` - Workbook with sheets `gschoolar_resultsPeriod_abs` and `Sheet1`; OOXML metadata shows a query-backed table `gschoolar_resultsPeriod_abs` with 11 columns and range `A1:K6770`.

**Intermediate and final local artifacts:**
- `abstracs_cleaned.csv` - 7,092 rows, 6 columns: `Title, Authors, Citations_counts, Year, Journal, Abstract`.
- `seed_labeled.csv` - 40 rows, 3 columns: `text, label_text, label`.
- `seed_generated.csv` - 500 rows, 3 columns: `text, label_text, label`.
- `abstracts_reclasificados_top15.csv` - 7,092 rows, 10 columns; adds `Predicted_Label`, `Confidence`, and automatic topic fields.
- `abstracts_clasificados_subtemas_aprobados.csv` - 7,092 rows, 13 columns; adds curated topic labeling fields.
- `abstracts_clasificados_filosóficos.csv` - 7,092 rows, 13 columns; final philosophy-topic labeling stage used by the methodology classifier.
- `abstracts_con_metodologia_optimizado.csv` - 7,092 rows, 14 columns; adds `Methodology`.

## Configuration

**Environment:**
- No repository-level manifest was detected in `requirements.txt`, `pyproject.toml`, `Pipfile`, `environment.yml`, or `.python-version`.
- Runtime configuration is embedded in notebook cells and depends on whichever environment executes them.
- GPU availability is optional and detected dynamically with `torch.cuda.is_available()`.

**Build:**
- No build system detected.
- No CI configuration detected.
- Output paths are hardcoded inside notebooks, for example `./googleScholarPeriodAbs.xlsx`, `./abstracs_cleaned.csv`, `classified_articles_setfit.csv`, and multiple `/content/drive/...` paths in the legacy Colab notebook.

## Platform Requirements

**Development:**
- Python environment with notebook support.
- `pip` access to install the libraries referenced in `AbstractsV2.ipynb` and `old/Christian_Escobar_Abstract_Classification_fix2.ipynb`.
- Optional NVIDIA GPU/CUDA for practical execution speed; notebook cells explicitly target CUDA 12.1 wheel index for PyTorch.
- Internet access is required at runtime to download Hugging Face models unless cached beforehand.

**Production:**
- Not a packaged production application.
- Current deliverable is a notebook-driven offline data pipeline that reads local files and writes local CSV/HTML outputs.

---

*Stack analysis: 2026-03-24*
